# Asset Placeholders

This directory keeps game-owned assets separated from Expo bootstrap assets.

- `textures/`: sprite sheets and tile textures
- `audio/`: music and SFX
- `fonts/`: custom UI fonts

Current placeholders are stubs for pipeline wiring.

